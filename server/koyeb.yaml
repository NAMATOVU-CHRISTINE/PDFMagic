name: pdfmagic-api
type: web
env:
  - key: NODE_ENV
    value: production
  - key: PORT
    value: "8000"
regions:
  - fra
instance_types:
  - type: free
ports:
  - port: 8000
    protocol: http
routes:
  - path: /
    port: 8000
build:
  type: buildpack
run_command: npm start
